{% extends 'base.html.twig' %}

{% block title %}Localidad{% endblock %}

{% block body %}
    <h1>Localidad</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idlocalidad</th>
                <td>{{ localidad.idlocalidad }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ localidad.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_localidad_index') }}">back to list</a>

    <a href="{{ path('app_localidad_edit', {'idlocalidad': localidad.idlocalidad}) }}">edit</a>

    {{ include('localidad/_delete_form.html.twig') }}
{% endblock %}
