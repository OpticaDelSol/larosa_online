<form method="post" action="{{ path('app_localidad_delete', {'idlocalidad': localidad.idlocalidad}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ localidad.idlocalidad) }}">
    <button class="btn">Delete</button>
</form>
