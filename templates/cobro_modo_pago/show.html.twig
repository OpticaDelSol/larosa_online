{% extends 'base.html.twig' %}

{% block title %}CobroModoPago{% endblock %}

{% block body %}
    <h1>CobroModoPago</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdcobroModoPago</th>
                <td>{{ cobro_modo_pago.idcobroModoPago }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cobro_modo_pago_index') }}">back to list</a>

    <a href="{{ path('app_cobro_modo_pago_edit', {'idcobroModoPago': cobro_modo_pago.idcobroModoPago}) }}">edit</a>

    {{ include('cobro_modo_pago/_delete_form.html.twig') }}
{% endblock %}
