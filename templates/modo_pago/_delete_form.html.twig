<form method="post" action="{{ path('app_modo_pago_delete', {'idmodoPago': modo_pago.idmodoPago}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ modo_pago.idmodoPago) }}">
    <button class="btn">Delete</button>
</form>
