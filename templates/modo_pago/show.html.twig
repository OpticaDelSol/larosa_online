{% extends 'base.html.twig' %}

{% block title %}ModoPago{% endblock %}

{% block body %}
    <h1>ModoPago</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdmodoPago</th>
                <td>{{ modo_pago.idmodoPago }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ modo_pago.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_modo_pago_index') }}">back to list</a>

    <a href="{{ path('app_modo_pago_edit', {'idmodoPago': modo_pago.idmodoPago}) }}">edit</a>

    {{ include('modo_pago/_delete_form.html.twig') }}
{% endblock %}
