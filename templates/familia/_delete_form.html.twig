<form method="post" action="{{ path('app_familia_delete', {'idfamilia': familium.idfamilia}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ familium.idfamilia) }}">
    <button class="btn">Delete</button>
</form>
