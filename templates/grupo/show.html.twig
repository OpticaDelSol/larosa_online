{% extends 'base.html.twig' %}

{% block title %}Grupo{% endblock %}

{% block body %}
    <h1>Grupo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idgrupo</th>
                <td>{{ grupo.idgrupo }}</td>
            </tr>
            <tr>
                <th>PrecioComun</th>
                <td>{{ grupo.precioComun }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_grupo_index') }}">back to list</a>

    <a href="{{ path('app_grupo_edit', {'idgrupo': grupo.idgrupo}) }}">edit</a>

    {{ include('grupo/_delete_form.html.twig') }}
{% endblock %}
