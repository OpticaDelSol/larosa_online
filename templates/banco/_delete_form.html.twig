<form method="post" action="{{ path('app_banco_delete', {'idbanco': banco.idbanco}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ banco.idbanco) }}">
    <button class="btn">Delete</button>
</form>
