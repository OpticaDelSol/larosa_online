<form method="post" action="{{ path('app_imagen_delete', {'idimagen': imagen.idimagen}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ imagen.idimagen) }}">
    <button class="btn">Delete</button>
</form>
